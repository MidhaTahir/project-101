export enum RouteTypes {
  PRIVATE = "private",
  PUBLIC = "public",
}

export enum RoutePaths {
  ADMIN = "/admin",
  CUSTOMER = "/customer",
  DETAILS = "/details",
  LOGIN = "/",
  SIGNUP = "/sign-up",
  NOT_FOUND = "/not-found",
}
