import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#47A580",
    },
  },
  // typography: {
  //   fontFamily: '"Helvetica Now Display", "Overpass", sans-serif',
  // },
});

export default theme;
